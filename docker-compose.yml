version: '2'
services:
  documentserver:
    image: onlyoffice/4testing-documentserver-ie:latest
    ports:
    - "80:80"
  testing_project:
    build: ./
    tty: true
    environment:
      - DOCUMENTSERVER=http://documentserver
      - USE_JWT=no
    command: ["bash", "./wait_for_documentserver_start.sh"]


